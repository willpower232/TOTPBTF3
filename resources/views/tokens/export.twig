{% extends 'global.twig' %}
{% block main %}

	<h1>{{ token.title }}</h1>

	<div class="export">
		{{ token.getQRCode()|raw }}
	</div>

	<a class="button floating" href="{{ route('tokens.show', [token.id_hash]) }}">&#128065;</a>

{% endblock %}
